package nine.spades.time;

import java.time.LocalDateTime;
import java.time.ZonedDateTime;
import java.util.List;

public class Countdown {

    public static List<Integer> calculateCountdown(LocalDateTime now, LocalDateTime target) {
        // TODO: Implement countdown calculation logic
        return List.of(0, 0, 0, 0);
    }

    public static List<Integer> calculateCountdown(ZonedDateTime now, ZonedDateTime target) {
        // TODO: Implement countdown calculation logic with timezone support
        return List.of(0, 0, 0, 0);
    }
}